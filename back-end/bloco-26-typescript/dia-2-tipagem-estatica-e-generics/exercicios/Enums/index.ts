export enum EColors {
  preta,
  branca,
  vermelha,
  prata,
};

export enum EDoors {
  fEsquerda = 1,
  fDireita,
  tEsquerda,
  tfDireita,
};

export enum EDirection {
  left = 'left',
  right = 'right',
};
